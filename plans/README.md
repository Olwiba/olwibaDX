# Implementation Plans

Generated by the improve skill on 2026-06-11, against commit `0886dfb`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Sanitize skill slugs in `dx skills install` (path traversal) | P1 | S | — | TODO |
| 002  | Fix broken README usage examples | P2 | S | — | TODO |
| 003  | Add CI workflow (typecheck + build on push/PR) | P2 | S | — | TODO |
| 004  | `generatePreviews`: throw instead of `process.exit`; style alignment | P3 | S | — | TODO |
| 005  | Dedupe skills-manifest types between `cli.ts` and `skills.ts` | P3 | S | 001 (soft) | TODO |
| 006  | `types`-first export conditions; `eslint` peer optional | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 005 soft-depends on 001: both touch `src/cli.ts`/`src/skills.ts`; running 001 first avoids merge noise. 005 is still executable if 001 was skipped.
- All six plans are independent of the unplanned findings below and can land in any release.

## Findings audited but not yet planned (need a maintainer decision or a prerequisite)

- **Restore `src/env` module**: the v0.0.12 release commit (`c7c1ca3`) deleted `src/env/` + the `./env` export while CHANGELOG 0.0.12/0.0.13 still advertise the feature ("Parse annotated `.env.example` specs"). Recoverable via `git show 655e0d9` / `f8141a6`. Decision needed: restore the module (intended consumer was genesis-start) or strip the changelog entries.
- **Test infrastructure baseline**: zero tests; `tsc --noEmit` is the only gate. Pure functions (`parseFigletFont`, `composeAsciiText`, `getAsciiAccentColumns`, `encodeGif`, `parseColor`, `parseFlags`) are cheap bun-test targets. Prerequisite for the LZW work below.
- **Real LZW compression in the GIF encoder**: `lzwEncode` (`src/ascii-gif.ts:383`) emits literal codes only — README banner GIFs are 11–21 MB each across the ecosystem. High value, but should land behind golden-file tests from the baseline above.

## Findings considered and rejected

- Identical byte sizes of dark/light GIF pairs: not a bug — identical pixel data with a different fixed-size (768-byte) palette legitimately yields identical file lengths.
- `number[]` accumulation in `encodeGif` (high peak memory): works for its one-shot CLI use; fixing falls out of the LZW rewrite if/when that lands. Not worth standalone effort.
- `--source` accepting arbitrary URLs in `dx skills install`: by design (user-invoked, explicit flag). Only the filesystem write path needed hardening (plan 001).
